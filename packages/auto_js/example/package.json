{
  "name": "@auto_js/napi-example",
  "type": "module",
  "private": true,
  "scripts": {
    "configure": "cmake \"-DCMAKE_MODULE_PATH=$(npm exec auto_js_cmake_include)\" -G Ninja -B build",
    "clean": "cmake -B build --target clean",
    "build": "ninja -C build example"
  },
  "devDependencies": {
    "@auto_js/napi": "0.0.1"
  }
}

diff --git a/config/compiler/BUILD.gn b/config/compiler/BUILD.gn
index 6c15cc0ae..81727107d 100644
--- a/config/compiler/BUILD.gn
+++ b/config/compiler/BUILD.gn
@@ -2160,6 +2160,8 @@ config("default_warnings") {
       # https://swiftshader-review.googlesource.com/c/SwiftShader/+/57968 has
       # rolled into angle.
       cflags += [ "/wd4244" ]
+    } else {
+      cflags += [ "-Wno-deprecated-declarations" ]
     }
   } else {
     if (is_apple || is_android) {
@@ -2196,6 +2198,23 @@ config("default_warnings") {
         "-Wno-class-memaccess",
       ]
 
+      # nb: These for gcc
+      cflags_cc += [
+        "-Wno-attributes",
+        "-Wno-dangling-else",
+        "-Wno-dangling-reference",
+        "-Wno-invalid-constexpr",
+        "-Wno-return-local-addr",
+        "-Wno-return-type",
+        "-Wno-sign-compare",
+        "-Wno-stringop-overflow",
+        "-Wno-template-id-cdtor",
+        "-Wno-uninitialized",
+        "-Wno-unused-function",
+        # This one is an error, I guess
+        "-Wno-changes-meaning",
+      ]
+
       # -Wunused-local-typedefs is broken in gcc,
       # https://gcc.gnu.org/bugzilla/show_bug.cgi?id=63872
       cflags += [ "-Wno-unused-local-typedefs" ]
@@ -2214,6 +2233,15 @@ config("default_warnings") {
       # -Wpacked-not-aligned complains all generated mojom-shared-internal.h
       # files.
       cflags += [ "-Wno-packed-not-aligned" ]
+    } else {
+      # nb: These for clang
+      cflags_cc += [
+        "-Wno-deprecated-builtins",
+        "-Wno-deprecated-declarations",
+        "-Wno-unnecessary-virtual-specifier",
+        "-Wno-unused-const-variable",
+        "-Wno-unused-function",
+      ]
     }
   }
 
